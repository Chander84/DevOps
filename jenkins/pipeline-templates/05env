pipeline {
    agent any

    environment {
        NAME = 'CHANDAAR'
        LASTNAME = 'KONKKA'
    }

    stages {
        stage('Build') {
            steps {
                sh 'echo $NAME $LASTNAME'
            }
        }
    }
}
