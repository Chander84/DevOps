pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                retry(3) {
                    sh 'echo hello'
                }

                timeout(time: 2, unit: 'MINUTES') {
                    sh 'sleep 1'
                }
            }
        }
    }
}
